li.list-group-item data-id=element.id
  span.sort
    i.glyphicon.glyphicon-sort
  = element.label

  .control-bar
    - if element.required
      span.label.label-primary required
    span.label.label-info = element.data_type

    = link_to form_form_element_path(form, element),
      { method: :delete,
        remote: true,
        data: { confirm: t('common.confirm') } } do
      small
        span.glyphicon.glyphicon-remove

