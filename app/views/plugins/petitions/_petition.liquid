{% if plugins.petition[ref].active %}
  <div class="action-bar action-bar--elevated action-bar--mobile-view--closed">
    <div class="action-bar__content">
      <div class="action-bar__top">
        <div class="action-bar__mobile-ui">
          <a class="action-bar__close-button">&#x2715;</a>
        </div>
        <h2>Sign the petition</h2>
        <div class="action-bar__target">
          {% unless plugins.petition[ref].target == blank %}
            TO: {{ plugins.petition[ref].target }}
          {% endunless %}
        </div>
        <p class="action-bar__petition-text">
          {{ plugins.petition[ref].description }}
        </p>
      </div>
      <div class="action-bar__main">
        <div class="action-bar__expand-arrow"></div>
        <div class="action-bar__shading"></div>

        <div class="mobile-hide">
          {% include 'Thermometer' %}
        </div>
        {% include 'Form',
          cta: plugins.petition[ref].cta,
          form_id: plugins.petition[ref].form_id,
          outstanding_fields: plugins.fundraiser[ref].outstanding_fields,
          fields: plugins.petition[ref].fields %}
      </div>
    </div>
  </div>

  <div class="action-bar__mobile-ui">
    <div class="action-bar__mobile-ui__bottom-bar">
      <button class="button action-bar__open-button">{{ plugins.petition[ref].cta }}</button>
    </div>
  </div>

  <script type="text/javascript">
    $(document).ready(function(){
      var myActionBar = new window.ActionBar();
    });
  </script>
{% endif %}
