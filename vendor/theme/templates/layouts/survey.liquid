{% comment %} Description: A page for surveying members. {% endcomment %}
{% comment %} Primary layout: true {% endcomment %}

{% include 'Top Bar', no_fade: true %}

<div class="survey">
  <div class="imageless-title">
    <div class="typography__highlight-wrapper survey-typo-high-wrapper">
      <h1 class="typography__highlight">
        <span>{{ title }}</span>
      </h1>
    </div>
  </div>
  <div class="body-text survey__intro">
    {{ content }}
  </div>
  {% include 'Survey' %}
</div>

{% include 'Simple Footer' %}
