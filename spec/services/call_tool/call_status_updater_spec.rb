require 'rails_helper'

describe CallTool::CallStatusUpdater do
  describe '.start!' do
    it 'does nothing if call already started' do
    end

    it 'sets status to "started" if call is unstarted' do
    end

    it 'publishes an event to update the call' do
    end
  end
end
